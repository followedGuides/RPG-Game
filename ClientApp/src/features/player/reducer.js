

const initalState = {
    position:[0,0],
}


const playerReducer = (state = initalState, action) => {
    switch (action.type) {
        default:
            return state
    }
}


export default playerReducer